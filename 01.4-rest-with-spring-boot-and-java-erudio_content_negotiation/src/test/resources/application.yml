#spring.application.name=01-rest-with-spring-boot-and-java-erudio

# src/test/resources/application.yml
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false # Se você não tem migrations H2, é melhor desativar o Flyway nos testes
  jpa:
    hibernate:
      ddl-auto: update